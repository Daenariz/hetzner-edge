name: Flake Check

on:
  pull_request:
    branches:
      - master
      - develop

jobs:
  flake-check:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
      - run: nix flake check
